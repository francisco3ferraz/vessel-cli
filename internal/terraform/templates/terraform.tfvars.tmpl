# Generated by vessel-cli — values populated from CLI flags and state.json.

app_name     = "{{.AppName}}"
aws_region   = "{{.AWSRegion}}"
image_tag    = "{{.ImageTag}}"
allowed_cidr = "{{.AllowedCIDR}}"
app_port     = {{.Port}}

env_vars = [{{range .EnvVars}}
  { name = {{.Name | hclQuote}}, value = {{.Value | hclQuote}} },{{end}}
]
